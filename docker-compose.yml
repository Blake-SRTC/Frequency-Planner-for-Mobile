version: "3"

services:
    mobile:
        container_name: mobile
        restart: always
        build: .
        ports: 
            - "6000:6000"
        environment:
            - TZ=America/La_Paz
        volumes:
            - .:/usr/src/app
